# 轨迹评估优化方案

基于 [`trajectory_eval_report.md`](../../../eval/trajectory/trajectory_eval_report.md)（1000 条，规则评估）的发现给出。
本方案只动「决策/工具选择」这一环，**不重构图结构**（路径已 98.9% 正确）。

---

## 一、诊断结论

| 维度 | 修复前（qwen3.7-max-preview） | 修复后（qwen3.7-max-2026-06-08） | 结论 |
|---|---|---|---|
| 节点路径 route | 98.90% | 98.40% | 编排骨架正确 |
| 意图 intent | 88.20% | 87.70% | 未改，仍 ~12% 误差（见 P3） |
| 槽位抽取 | 100.00% | 100.00% | 订单号抽取正确 |
| 缺失槽位 | 100.00% | 100.00% | 缺槽识别正确 |
| 澄清行为 | 99.40% | 99.40% | 该追问时追问了 |
| **动作/分支 action** | **66.50%** | **95.80%** | **P0 已修复（工具描述消歧）** |
| **轨迹总准确率** | **61.10%** | **86.40%** | 被 action 拉低 → 已大幅回升 |
| 关键事实 key_facts | 29.40% | **69.30%** | 工具选对后事实落地（答案评估） |
| LLM 裁判通过率 | 37.40% | **75.30%** | 均分 0.472 → 0.786（答案评估） |

**修复前失败模式**（报告「未过维度：动作」逐条印证，已消除）：
- `order_query` → 误调 `query_logistics`（应 `query_order`）
- `logistics` → 误调 `query_order`（应 `query_logistics`）
- `refund_request` / `refund_consult` → 误调 `create_handoff` 或 `rag_retrieve`（应 `request_refund` / `rag_retrieve`）
- `multiturn_followup`：意图准确率仅 2.5%，意图先错 → 工具随之全错

**修复后残余失败模式（答案评估裁判理由印证，见新 P1）**：
- `refund_consult`（`consult_policy`）：已正确选 `rag_retrieve`，但**检索返回弱/空**，
  回复缺「七天无理由 / 原路退回」等事实点 → 裁判判未过；
- 个别 `refund_consult` 误入 `no_reason_return` 子意图并实际发起退货，而非只答政策；
- 意图误识别（~12%）跨单点评估连带，未处理。

---

## 二、根因

1. **工具描述才是 function calling 的真实决策输入，而当前描述有歧义/过宽**。
   工具信息通过 `tools=` 参数（结构化 schema）注入 LLM，并不在系统提示词里罗列
   （见 `build_agent_system_prompt` 注释「可调用工具通过 tools= API 参数下发，不在此罗列」）；
   LLM 选哪个函数主要看各工具的 `description` + `parameters`。当前 `query_order` /
   `query_logistics` 描述重叠、`create_handoff` 过宽，导致选错。**修描述即可，
   无需在系统提示词里再重复一份路由表**（那会与 schema 形成双源、且提示词本就不罗列工具）。
2. **`query_order` 与 `query_logistics` 描述重叠**：都吃 `order_id`，都含"发货"语义。
   "发货了吗"类问题二义（已发货=订单状态 vs 运输中=物流），模型易选错。
3. **`create_handoff` 描述过宽**：退款类（缺订单号或不确定）频繁误触发转人工。
4. **工具选择走 LLM function calling，非确定性**：temperature 0.3 带来方差
   （答案评估已观察到同 case 浮动）。
5. **多轮跟进承接不稳**：`slot_followup` 在裸订单号样本上未稳定生效，意图先错，
   工具随之错（跨单点评估的连带问题）。

涉及文件：
- `app/business/prompts/system.py` → `build_agent_system_prompt`
- `app/business/tools/tool_executor.py` → `TOOL_SCHEMAS` 描述
- `app/business/agent/agent_node.py` → 决策温度 / 硬护栏
- `app/config/llm.py` → `LLMConfig.generation_kwargs`
- `app/business/intent/routing.py` → `slot_followup` 分支（多轮）

---

## 三、优化方案（按收益 / 风险排序）

### P0 — 强化工具元信息描述（最高收益，低风险，主修复手段）
工具选择由 `tools=` schema 驱动，因此**只改 `TOOL_SCHEMAS` 的 `description` / `parameters`**
即可，不动系统提示词（提示词继续只传意图/槽位上下文，不重复罗列工具路由）。

1. **消歧 `query_order` / `query_logistics`**：
   - `query_order`：聚焦**订单本身**——是否下单 / 是否已发货 / 待付款 / 商品 / 金额。
     触发语："发货了吗 / 订单状态 / 订单详情 / 金额"。描述去掉"发货情况"歧义，改"是否已发货"。
   - `query_logistics`：聚焦**已发货后的运输过程**——快递到哪了 / 运输中 / 派送中 /
     是否签收。触发语："物流 / 快递 / 配送进度"。
   - 在两者描述里互相划界：**"已发货"属订单状态 → `query_order`；"运输中/派送中"属物流 → `query_logistics`**。
2. **收紧 `create_handoff` 触发条件**：
   描述明确"**仅当**用户明确要求人工 / 投诉情绪升级 / 多次澄清失败才调用；
   **不因缺订单号或信息不全而转人工**——先用业务工具，由工具提示用户补全"。
   降低退款类误转人工。
3. **分流 `request_refund` / `rag_retrieve`（after_sale_refund）**：
   - `request_refund`：强调"用户要**实际办理**退款/退货/换货/维修（而非仅咨询政策）时调用"；
   - `rag_retrieve`：增加"退换货政策、七天无理由等知识咨询"触发语，使纯咨询走知识检索而非转人工。

预期：订单/物流互调、退款误转人工基本消除，动作准确率大幅回升。

> **状态：P0 已落地并验证。** 改动位于 `app/business/tools/tool_executor.py`
> （`query_order` / `query_logistics` / `create_handoff` / `request_refund` 描述）与
> `app/business/tools/rag_tool.py`（`RagRetrieveTool.description`）。复跑 1000 条：
> 轨迹 `action` 66.50% → **95.80%**、总准确率 61.10% → **86.40%**；答案 `key_facts`
> 29.40% → **69.30%**、`action` 56.40% → **95.80%**、LLM 裁判 37.40% → **75.30%**。
> 工具选择瓶颈已消除。

> 注：系统提示词（`build_agent_system_prompt`）保持现状即可——它已把
> `当前意图` / `已填槽位` 透传给 LLM，工具选择交给 schema 描述驱动。若描述优化后
> 个别样本仍选错，再考虑在调度节点补**一句**轻量约束（如"按当前意图选择对应工具"），
> 但不必重复整套路由表（避免与 schema 双源维护）。

### P1 — RAG 检索质量（退款政策咨询，新瓶颈）
工具选择已正确（`consult_policy` 稳定选 `rag_retrieve`），但答案评估裁判理由反复指出：
回复**缺政策事实点**（七天无理由、原路退回等），根因在**检索返回弱/空**而非选错工具。
这已成为 `key_facts`（69%）与裁判通过率（75%）的主要残余缺口。

落地方向（需在 `app/business/rag/` 与知识库内容侧排查，非本次 schema 修复范围）：
1. **知识库覆盖**：确认退款/退换货政策 FAQ 已入库（`POST /knowledge/upload`），且
   chunk 切分（`chunk_size` 800 / `overlap` 100）未把「七天无理由」「原路退回」等关键句切碎；
2. **检索召回**：`retrieval_strategy` 当前 `bm25`、`min_score_threshold` 5.0。对短政策问句，
   可评估 `semantic` / `hybrid` + rerank（`rag_config.rerank.enabled`）提升召回；
3. **回复合成**：`response_generator` 在拿到 `rag_retrieve` 空/弱结果时，是否应回退到
   更明确的"暂无命中，请补充"而非含糊带过——与 `response_prompts.yml` 模板配合。

预期：若检索能稳定命中政策原文，`key_facts` 与裁判通过率可再上一个台阶。

### P2 — 决策温度确定性
为 `agent_node` 的工具决策步**单独固定 `temperature=0`**（或 0.1），与回复生成温度解耦。
实现：在 `LLMConfig` 增加 `agent_temperature` 字段，`agent_node` 构造时覆盖
`generation_kwargs["temperature"]`。收益：消除同 case 浮动。需确认回复生成仍可用较高温度。

### P3 —（可选 / 实验）意图 → 工具的硬护栏
在 `agent_node` 中：当 `current_main_intent ∈ {order_query, logistics}` 且 `slots`
有 `order_id` 时，**直接调用对应工具、跳过 LLM function calling**。保证最高频且最易混淆
的两类 100% 正确。风险：降低 ReAct 灵活性，建议作为护栏 / fallback 而非默认，
先以 A/B 对照评估验证再决定是否常驻。

### P3 — 多轮跟进意图承接（意图路由侧，跨评估）
`multiturn_followup` 意图准确率仅 2.5%，是该类动作全失的根。排查 `slot_followup`：
- 确认轨迹评估 `run_target` 的 `update_state`（写入 `previous_sub_intent`）在
  checkpointer 读取时序上正确生效；
- 或在 `IntentRouterService` 强化：order_id 命中 + `previous_sub_intent ∈ 跟进集`
  → **强制** `slot_followup`，避免被 LLM 兜底覆盖。

---

## 四、验证方法

改后用同一 1000 条轨迹评估复跑：

1. **主指标**：`action_accuracy`（目标 ≥ 95%）、`trajectory_overall`（目标 ≥ 90%）；
2. **辅助**：`route` / `intent` / `slot_extraction` 维持高位；响应时间不显著恶化；
3. **联动答案评估**：订单 / 物流误调修复后，应能在 `answer` 评估里落地事实点
   （"已发货/智能客服机器人 Pro" 等），带动 `key_facts` 与 LLM 裁判分提升；
4. **多轮专项**：单独看 `multiturn_followup` 的 `intent` / `action` 是否回升。

复跑命令：
```bash
python3 eval/trajectory/run_eval.py --max-concurrency 6
python3 eval/answer/run_eval.py     # 联动确认答案质量
```

---

## 五、优先级与工作量

| 项 | 内容 | 改动量 | 收益 | 风险 | 状态 |
|---|---|---|---|---|---|
| P0 | 强化工具元信息描述（query_order/logistics 消歧 + handoff 收紧 + refund/rag 分流） | ~20 行 | 高 | 低 | **已完成并验证** |
| P1 | RAG 检索质量（退款政策咨询召回 + 知识库覆盖 + 回复合成回退） | rag + knowledge | 高 | 中 | 待做（新瓶颈） |
| P2 | 决策温度固定 | config + agent_node | 中 | 低 | 可选 |
| P3 | 意图→工具硬护栏 | agent_node | 高(局部) | 中 | 可选 |
| P3 | 多轮 slot_followup 承接 | routing | 中 | 中 | 待做（意图 ~12% 误差） |

**建议顺序**：P0（✅ 已落地，不动提示词）→ P1（修检索质量，攻残余 25% 裁判缺口） →
P2（去方差）→ P3（意图/多轮修复，攻残余 ~12% 意图误差）。
